<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sign In - Shopzilla</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { font-family: 'Inter', sans-serif; background: #f8fafc; margin: 0; color: #333; }
    .auth-container { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 80vh; }
    .auth-card {
      background: #fff; padding: 2.5rem 2rem; border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      width: 90%; max-width: 400px; text-align: center;
    }
    h2 { margin-bottom: 0.5rem; font-size: 1.5rem; }
    p.subtitle { color: #666; margin-bottom: 1.5rem; font-size: 0.95rem; }
    input[type="email"], input[type="password"] {
      width: 100%; padding: 10px; margin-bottom: 1rem;
      border: 1px solid #ccc; border-radius: 6px;
    }
    .remember-forgot {
      display: flex; justify-content: space-between; align-items: center;
      font-size: 0.9rem; margin-bottom: 1rem;
    }
    .remember-forgot a { color: #007bff; text-decoration: none; }
    .btn-primary {
      width: 100%; background: #007bff; color: white; border: none;
      padding: 10px 0; border-radius: 6px; font-weight: 500; cursor: pointer;
    }
    .btn-primary:hover { background: #0056b3; }
    .divider { margin: 1.5rem 0; border-bottom: 1px solid #ddd; position: relative; }
    .divider span {
      position: absolute; top: -10px; left: 50%; transform: translateX(-50%);
      background: #fff; padding: 0 10px; color: #888; font-size: 0.85rem;
    }
    .social-buttons { display: flex; gap: 10px; }
    .social-buttons button {
      flex: 1; border: 1px solid #ccc; border-radius: 6px;
      padding: 8px 0; background: #fff; cursor: pointer; font-weight: 500;
    }
    .social-buttons button:hover { background: #f0f0f0; }
    .auth-footer { font-size: 0.9rem; margin-top: 1rem; }
    .auth-footer a { color: #007bff; text-decoration: none; }
  </style>
</head>
<body>
  <header class="navbar">
    <div class="logo">Shopzilla</div>
    <nav>
      <a href="/">Home</a>
      <a href="/products">Products</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
    </nav>
    <div class="nav-right">
      <a href="/cart" class="cart-btn">🛒 Cart (0)</a>
      <a href="/login" class="btn-outline">Sign In</a>
      <a href="/register" class="btn-primary">Sign Up</a>
    </div>
  </header>

  <div class="auth-container">
    <div class="auth-card">
      <h2>Welcome Back</h2>
      <p class="subtitle">Sign in to your account to continue shopping</p>


     <% if (typeof successMessage !== 'undefined' && successMessage) { %>
  <div style="color: #1e7e34; background: #d4edda; border: 1px solid #c3e6cb; padding: 10px; border-radius: 6px; margin-bottom: 1rem;">
    <%= successMessage %>
  </div>
<% } %>

<% if (typeof error !== 'undefined' && error) { %>
  <div style="color: #ff4d4d; background: #ffe6e6; border: 1px solid #ffcccc; padding: 10px; border-radius: 6px; margin-bottom: 1rem;">
    <%= error %>
  </div>
<% } %>
      <form action="/login" method="POST">
  <input type="email" name="email" placeholder="Enter your email" required>
  <input type="password" name="password" placeholder="Enter your password" required>
  
  <div class="remember-forgot">
    <label><input type="checkbox"> Remember me</label>
    <a href="#">Forgot password?</a>
  </div>

  <button type="submit" class="btn-primary">Sign In</button>
</form>
      <div class="auth-footer">
        Don’t have an account? <a href="/register">Sign up</a>
      </div>

      <div class="divider"><span>or continue with</span></div>

      <div class="social-buttons">
        <button>🔴 Google</button>
        <button>🔵 Facebook</button>
      </div>
    </div>
  </div>

  <footer>
    <div>
      <h3>Shopzilla</h3>
      <p>Your trusted online shopping destination for premium products. We offer quality items with fast shipping and excellent customer service.</p>
    </div>
    <div>
      <h3>Quick Links</h3>
      <a href="/products">Products</a><br>
      <a href="/about">About Us</a><br>
      <a href="/contact">Contact</a><br>
    </div>
  </footer>
  <div class="footer-bottom">© 2025 Shopzilla. All rights reserved.</div>
  <script>
  document.addEventListener('DOMContentLoaded', () => {
    const googleBtn = document.querySelector('.social-buttons button:first-child');
    const facebookBtn = document.querySelector('.social-buttons button:last-child');

    googleBtn.addEventListener('click', () => {
      alert('Google sign-in feature coming soon!');
      // later: window.location.href = '/auth/google';
    });

    facebookBtn.addEventListener('click', () => {
      alert('Facebook login feature coming soon!');
      // later: window.location.href = '/auth/facebook';
    });
  });
</script>
</body>
</html>